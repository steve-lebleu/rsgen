const {} = require('./../bin/rsgen');
const { expect } = require('chai');

describe('Package', () => {

  before(() => {});

  after(() => {});

  describe('Package composition', () => {

    it('should expose a command rsgen', () => {

    });

  });

});