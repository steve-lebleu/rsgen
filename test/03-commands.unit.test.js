const fs = require('fs');

const { expect } = require('chai');

describe('Commands', () => {

  before(() => {});

  after(() => {});

  describe('Generate', () => {

    it('should expose a command run', () => {
     
    });

  });

});